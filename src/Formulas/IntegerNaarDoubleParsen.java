package Formulas;

public class IntegerNaarDoubleParsen {
	
	/*
	 * Dit is de methode waarmee je de wrapper klasses van Double én Integer
	 * naar een double kunt parsen.
	 * 
	if(input.get(0) instanceof Double || input.get(0) instanceof Integer)
	{
		double inputdouble = 0;
		if(input.get(0) instanceof Integer)
		{
			inputdouble = (double) ((Integer) input.get(0)).intValue();
		}
		if(input.get(0) instanceof Double)
		{
			inputdouble = (double) input.get(0);
		}
	*/
}
